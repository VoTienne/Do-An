import { COLOURS } from "../database/Database";

const theme = {
    light: {
        theme:'light',
        color:COLOURS.black,
        background:COLOURS.white,
        blue:COLOURS.blue
    },
    dark: {
        theme:'dark',
        color:'white',
        background:'black',
        blue:COLOURS.red
    }
}

export default theme;